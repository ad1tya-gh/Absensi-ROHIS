services:
  - type: web
    name: absen-smk-global
    env: php
    region: sin
    buildCommand: "composer install --no-dev --optimize-autoloader && npm install && npm run build"
    startCommand: "heroku-php-apache2 public/"
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false